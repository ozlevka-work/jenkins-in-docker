#!/bin/bash -x

#-v /usr/lib/x86_64-linux-gnu:/usr/lib/x86_64-linux-gnu:ro \

docker run -d \
	--restart unless-stopped \
	--name jenkins \
	-p 8080:8080 \
	-p 5000:5000 \
	-v $HOME/tmp/jenkins-home:/var/jenkins_home \
	--privileged=true \
	test-jenkins:latest

